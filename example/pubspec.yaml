name: safe_setstate_example
description: Example app demonstrating flutter_safe_setstate package
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_safe_setstate:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
